# Detent CLI Memories

## Tool Usage
- Always use `detent <command>` to run the CLI locally (e.g., `detent check --force`)
- AI runs in verbose mode automatically (no TUI) - only humans see the TUI
- Use `--force` flag with check command to skip cache
